# rhythm-seek
An API that takes in a playlist (just spotify for now) and a sample audio, and returns the top X songs in the playlist that sound the closest to the sample audio. The playlist can be stored for future queries.
